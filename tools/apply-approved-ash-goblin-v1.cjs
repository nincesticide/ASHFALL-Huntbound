#!/usr/bin/env node
'use strict';
const fs=require('fs');
const path=require('path');
const file=path.resolve(__dirname,'..','js','game.js');
let src=fs.readFileSync(file,'utf8');
const marker='/* ===== approved Ash Goblin animated sprite v1 =====';
if(src.includes(marker)){
  console.log('Approved Ash Goblin v1 is already integrated.');
  process.exit(0);
}
const runtime="/* ===== approved Ash Goblin animated sprite v1 =====\n   Visual-only mapping for standard goblin and gutterling enemies.\n   The Gutter King / bossgoblin remains on its dedicated boss atlas. */\nconst APPROVED_GOBLIN_ATLAS_V1=new Image();\nAPPROVED_GOBLIN_ATLAS_V1.decoding='async';\nAPPROVED_GOBLIN_ATLAS_V1.src='assets/monsters/common/ash_goblin/ash_goblin.atlas.png';\nlet approvedGoblinAtlasReadyV1=false;\nAPPROVED_GOBLIN_ATLAS_V1.onload=()=>approvedGoblinAtlasReadyV1=true;\nAPPROVED_GOBLIN_ATLAS_V1.onerror=()=>approvedGoblinAtlasReadyV1=false;\nconst APPROVED_GOBLIN_ROWS_V1={\n  idle:{south:0,north:1,east:2,west:3},\n  walk:{south:4,north:5,east:6,west:7},\n  attack:{south:8,north:9,east:10,west:11},\n  special:{south:12,north:13,east:14,west:15},\n  hit:{south:16,north:17,east:18,west:19},\n  death:{south:20,north:21,east:22,west:23}\n};\nconst APPROVED_GOBLIN_ANIMS_V1={\n  idle:{frames:4,fps:5,loop:true},\n  walk:{frames:6,fps:10,loop:true},\n  attack:{frames:6,fps:12,loop:false},\n  special:{frames:6,fps:10,loop:false},\n  hit:{frames:3,fps:12,loop:false},\n  death:{frames:6,fps:8,loop:false,hold:550}\n};\nconst approvedGoblinMotionV1=new Map(),approvedGoblinDeathsV1=[];\nfunction approvedGoblinKindV1(e){return !!e&&!e.boss&&(e.kind==='goblin'||e.kind==='gutterling')}\nfunction approvedGoblinFacingV1(dx,dy,fallback='south'){\n  if(Math.abs(dx)>Math.abs(dy))return dx<0?'west':'east';\n  if(Math.abs(dy)>0)return dy<0?'north':'south';\n  return fallback\n}\nfunction approvedGoblinVisualV1(e,now=performance.now()){\n  const key=e.id||`${e.kind}:${e.x}:${e.y}`,tx=e.x,ty=e.y;\n  let s=approvedGoblinMotionV1.get(key);\n  if(!s){s={fromX:tx,fromY:ty,toX:tx,toY:ty,started:now,duration:1,facing:'south',anim:'idle',animStarted:now};approvedGoblinMotionV1.set(key,s)}\n  const priorT=clamp((now-s.started)/Math.max(1,s.duration),0,1),cx=s.fromX+(s.toX-s.fromX)*priorT,cy=s.fromY+(s.toY-s.fromY)*priorT;\n  if(tx!==s.toX||ty!==s.toY){\n    const dx=tx-s.toX,dy=ty-s.toY;\n    s.fromX=cx;s.fromY=cy;s.toX=tx;s.toY=ty;s.started=now;s.duration=225;\n    s.facing=approvedGoblinFacingV1(dx,dy,s.facing)\n  }\n  if((e.attackDx||e.attackDy))s.facing=approvedGoblinFacingV1(e.attackDx||0,e.attackDy||0,s.facing);\n  const t=clamp((now-s.started)/Math.max(1,s.duration),0,1),x=s.fromX+(s.toX-s.fromX)*t,y=s.fromY+(s.toY-s.fromY)*t,moving=t<1;\n  const wallNow=Date.now(),hitAge=wallNow-(e.hitFlash||0),attackAge=wallNow-(e.attackFlash||0);\n  let anim='idle',animStarted=s.animStarted;\n  if(hitAge>=0&&hitAge<330){anim='hit';animStarted=e.hitFlash}\n  else if(attackAge>=0&&attackAge<(e.kind==='gutterling'?660:540)){anim=e.kind==='gutterling'?'special':'attack';animStarted=e.attackFlash}\n  else if(moving){anim='walk';animStarted=s.started}\n  if(anim!==s.anim){s.anim=anim;s.animStarted=animStarted||now}\n  return{x,y,facing:s.facing,anim,animStarted:animStarted||s.animStarted||now}\n}\nfunction approvedGoblinFrameV1(anim,started,now=performance.now()){\n  const spec=APPROVED_GOBLIN_ANIMS_V1[anim]||APPROVED_GOBLIN_ANIMS_V1.idle,elapsed=Math.max(0,now-(started||now)),raw=Math.floor(elapsed/1000*spec.fps);\n  return spec.loop?raw%spec.frames:Math.min(spec.frames-1,raw)\n}\nfunction drawApprovedGoblinAtlasFrameV1(e,state,anim,started,now=performance.now(),alpha=1){\n  if(!approvedGoblinAtlasReadyV1)return false;\n  const spec=APPROVED_GOBLIN_ANIMS_V1[anim]||APPROVED_GOBLIN_ANIMS_V1.idle,row=APPROVED_GOBLIN_ROWS_V1[anim]?.[state.facing]??APPROVED_GOBLIN_ROWS_V1[anim]?.south??0,frame=approvedGoblinFrameV1(anim,started,now);\n  const sx=frame*96,sy=row*96,dw=64,dh=64,centerX=state.x*TILE+16,groundY=state.y*TILE+29,dx=Math.round(centerX-dw/2),dy=Math.round(groundY-(87/96)*dh);\n  ctx.save();ctx.globalAlpha=alpha;ctx.imageSmoothingEnabled=false;\n  if(Date.now()-(e.hitFlash||0)<125){ctx.shadowColor='#fff';ctx.shadowBlur=8;ctx.filter='brightness(1.28)'}\n  ctx.drawImage(APPROVED_GOBLIN_ATLAS_V1,sx,sy,96,96,dx,dy,dw,dh);\n  ctx.filter='none';ctx.shadowBlur=0;ctx.restore();\n  return true\n}\nfunction drawApprovedGoblinSpriteV1(e,now=performance.now()){\n  if(!approvedGoblinKindV1(e)||!approvedGoblinAtlasReadyV1)return false;\n  const state=approvedGoblinVisualV1(e,now);\n  drawApprovedGoblinAtlasFrameV1(e,state,state.anim,state.animStarted,now);\n  const x=e.x*TILE,y=e.y*TILE;\n  ctx.save();\n  if(e.elite){ctx.strokeStyle=e.trait==='ZONE LIEUTENANT'?'#f0c768':e.golden?'#ffd85b':e.nemesis?'#ef5656':'#b87ef0';ctx.lineWidth=2;ctx.strokeRect(x+2,y+2,28,28);ctx.lineWidth=1}\n  if(e.id===selectedTargetId){ctx.strokeStyle='#7fe3ef';ctx.lineWidth=2;ctx.strokeRect(x+1,y+1,30,30);ctx.lineWidth=1}\n  ctx.fillStyle='#151922';ctx.fillRect(x+4,y+1,24,4);ctx.fillStyle=e.elite?'#c16ce4':'#d85d68';ctx.fillRect(x+4,y+1,24*clamp(e.hp/e.maxHp,0,1),4);ctx.strokeStyle='rgba(255,255,255,.12)';ctx.strokeRect(x+4.5,y+1.5,23,3);\n  ctx.restore();return true\n}\nfunction queueApprovedGoblinDeathV1(e){\n  if(!approvedGoblinKindV1(e)||e._approvedGoblinDeathQueuedV1)return;\n  e._approvedGoblinDeathQueuedV1=true;\n  const s=approvedGoblinMotionV1.get(e.id)||{facing:approvedGoblinFacingV1(e.attackDx||0,e.attackDy||0,'south')};\n  approvedGoblinDeathsV1.push({id:e.id,kind:e.kind,x:e.x,y:e.y,facing:s.facing||'south',started:performance.now(),elite:!!e.elite,golden:!!e.golden,nemesis:!!e.nemesis,trait:e.trait||null});\n}\nfunction drawApprovedGoblinDeathsV1(now=performance.now()){\n  for(let i=approvedGoblinDeathsV1.length-1;i>=0;i--){\n    const d=approvedGoblinDeathsV1[i],spec=APPROVED_GOBLIN_ANIMS_V1.death,life=spec.frames/spec.fps*1000+(spec.hold||0),age=now-d.started;\n    if(age>life){approvedGoblinDeathsV1.splice(i,1);approvedGoblinMotionV1.delete(d.id);continue}\n    const state={x:d.x,y:d.y,facing:d.facing},ghost={hitFlash:0};\n    drawApprovedGoblinAtlasFrameV1(ghost,state,'death',d.started,now,Math.max(.45,1-age/life*.45))\n  }\n}";
const rendererAnchor='function drawZoneMonsterSpriteV09(e,now=performance.now()){const k=e.kind;';
if(!src.includes(rendererAnchor))throw new Error('Renderer anchor changed; refusing to patch security/session source blindly.');
src=src.replace(rendererAnchor,runtime+'\n\nfunction drawZoneMonsterSpriteV09(e,now=performance.now()){if(drawApprovedGoblinSpriteV1(e,now))return true;const k=e.kind;');
const rewardAnchor="function rewardKill(run,killer,e){\n  if(e.rewarded)return;e.rewarded=true;";
if(!src.includes(rewardAnchor))throw new Error('rewardKill anchor changed; refusing to patch.');
src=src.replace(rewardAnchor,"function rewardKill(run,killer,e){\n  if(e.rewarded)return;e.rewarded=true;queueApprovedGoblinDeathV1(e);");
const drawAnchor='  drawExtraClassCombatEffects(run);\n  drawStatusFx(run);';
if(!src.includes(drawAnchor))throw new Error('Combat draw anchor changed; refusing to patch.');
src=src.replace(drawAnchor,'  drawExtraClassCombatEffects(run);\n  drawApprovedGoblinDeathsV1();\n  drawStatusFx(run);');
fs.writeFileSync(file,src);
console.log('Integrated approved Ash Goblin v1 for goblin + gutterling; bossgoblin untouched.');
